<<<<<<< HEAD
=======
{# on étend le template depuis base.html.twig, qui est donc son parent #}
{% extends "template.html.twig" %}

{# on veut surcharger le title, on utilise le bloc, on récupère le contenu du parent {{ parent() }} et on rajoute notre texte #}
{% block title %}{{ parent() }} | Accueil filiere{% endblock %}

{% block menuhaut %}

{% endblock %}

{% block centre %}
>>>>>>> 6fa3d48ea8d234d007d7cdcc867c8a2bb4c9fa6e
    <!-- Page Content -->
<main role="main" class="container">
        <div class="row">
            <div class="col-lg-12 text-center">
                <h1 class="mt-5">Administration du CF2m</h1>
                <p class="lead">Gestion des sessions</p>               
            </div>
            <div <div class="col-lg-12 text-center">
<<<<<<< HEAD
            <h4><a href="?insert={{9}}">Ajouter une session</a></h4> 
=======
            <h4><a href="?insertlasession">Ajouter une session</a></h4> 
>>>>>>> 6fa3d48ea8d234d007d7cdcc867c8a2bb4c9fa6e
            </div>
            <br>
            <div class="col-lg-6 text-left">
                {# Faites apparaître les sections (celles du menu) cliquables dans un nouveau h4, sinon écrivez "Pas encore d'éléments dans le menu" #}
                {% for item in detailsession %}
<<<<<<< HEAD
                    <h4 class="mt-3">{{ item.lenom }} | <a href="?update={{ item.idlasession }}">modifier</a> | <a href="?delete={{ item.idlasession }}">supprimer</a></h4>
                    <p>{{ item.lacronyme }}</p>
                    <p>{{ item.lannee }}</p>
                    <p>{{ item.lenumero }}</p>
                    <p>{{ item.letype }}</p>
                    <p>{{ item.ledebut }}</p>
                    <p>{{ item.lafin }}</p>
                    <h6>Stagiaires:</h6>
                    {# création de 2 variables pour éclater (avec split) le nom et prénom des stagiaires #}
                    {% set lenom = item.lenom|split("|||") %}
                    
                    {% for detailsession in lenom %}
                        {# si vide #}
                        {% if detailsession is empty %}
                            <p>Pas de session</p>
                        {% else %}
                        {# on utilise loop.index0 pour prendre le prénom associé au nom sur lequel on fait la boucle #}
                        <p>{{ detailsession }} {{ lenom[loop.index0] }}</p>
                        {%  endif %}
                    {% endfor %}

                {% else %}
                <h4 class="mt-3">Pas encore de session sur notre page accueil</h4>
                {% endfor %}
            </div>
        </div>
</main>
=======
                    <h4 class="mt-3">{{ item.lenom }} | <a href="?updatelasession={{ item.idlasession }}">modifier</a> | <a onclick="if(window.confirm('Voulez-vous vraiment supprimer cette session ?')) location='./?confirmationdeletelasession={{ item.idlasession }}';" href="#">supprimer</a></h4>
                    <p>{{ item.lacronyme }}</p>
					<p>Début: {{ item.debut }} | Fin: {{ item.fin }}</p>
                {% else %}
					<h4 class="mt-3">Pas encore de session sur notre page accueil</h4>
                {% endfor %}
            </div>
        </div>
</main>

{% endblock %}

{% block bas %}
{% include "footer.html.twig" %}
{% endblock %}
>>>>>>> 6fa3d48ea8d234d007d7cdcc867c8a2bb4c9fa6e
